Dont run the `npm run dev` or

```powershell
Stop-Process -Name "node" -Force -ErrorAction Ignore; Remove-Item -Recurse -Force .plasmo -ErrorAction Ignore; npm run dev
```

the user has it will keep running automatically
